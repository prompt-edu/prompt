[project]
name = "prompt-api-stress"
version = "0.1.0"
description = "PROMPT API stress & fuzz suite (k6 load + Python fuzzer)"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.28.1,<1.0",
]

[tool.uv]
package = false
