"""PROMPT API stress & fuzz suite orchestrator.

The full run is orchestrated by :mod:`stress.cli`, exposed as the
``api-stress-test`` console script (see ``pyproject.toml``). ``run.sh`` is a thin
back-compat shim over the same entry point.
"""
