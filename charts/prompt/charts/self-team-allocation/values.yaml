# Overridden by the umbrella `self-team-allocation:` block; defaults for standalone use.
enabled: true
server:
  enabled: true
  image: prompt-server-self-team-allocation
  replicas: 1
  port: 8080
  apiPath: /self-team-allocation/api
  dbKey: self-team-allocation
  rateLimited: true
client:
  enabled: true
  image: prompt-clients-self-team-allocation-component
  replicas: 1
  port: 80
  path: /self-team-allocation
  stripPrefix: true
